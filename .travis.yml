# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required 

addons:
  apt:
    packages:
      - libgdal-dev 
      - libproj-dev 
      - libxml2-dev
      - libcurl4-gnutls-dev 

notifications:
  email:
    on_success: change
    on_failure: change
